{
  "name": "smart-vendor-compliance-system",
  "version": "1.0.0",
  "description": "Smart Vendor Compliance and Loss Detection System for Walmart Sparkathon",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:mobile": "cd mobile && npm start",
    "dev:blockchain": "cd blockchain && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../mobile && npm install && cd ../blockchain && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  },
  "keywords": ["vendor", "compliance", "fraud-detection", "blockchain", "mern"],
  "author": "Your Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
